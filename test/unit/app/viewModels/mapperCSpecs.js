(function mapperCSpecsScope(sinon) {
    'use strict';

    describe('mapper Controller specs', function mapperCSpecs() {
        it('should create a new mapper', function createMapper() {
            // given

            // when

            // then

        });
    });
}(window.sinon));
