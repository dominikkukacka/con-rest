(function mapperCScope(angular) {
    'use strict';

    var app = angular.module('con-rest');

    app.controller('mapperC', function mapperC($scope) {
        $scope.name = null;
    });
}(window.angular));
